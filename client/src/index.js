import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// Places the main App component at the root of index.html
ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
